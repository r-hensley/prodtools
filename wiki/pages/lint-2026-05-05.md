---
title: Lint Report 2026-05-05
tags: [lint, maintenance]
sources: []
updated: 2026-05-05
---

# Lint Report — 2026-05-05

Run after ingest of `json2jobdef-staging-workflow` and `/stage-entry`
skill (2026-05-05).

## Summary
- 🔴 Errors: 3 (all pre-existing — same 3 broken slugs flagged in earlier lints)
- 🟡 Warnings: 0
- 🔵 Info: 2

## 🔴 Broken Links

All three were already present in lint-2026-04-22 and lint-2026-04-24.
Listed here for completeness; none introduced by today's ingest.

- `[[2026-04-21-pbi-sequence-implementation]]` — referenced 8× across
  pages (mostly from `pbi-sequence-workflow`, `lint-2026-04-21`,
  `lint-2026-04-22`). The slug exists only in `wiki/raw/`, not
  `wiki/pages/`.
  **Fix:** either publish a synthesized page from the raw notes, or
  rewrite the references as prose pointers to `raw/2026-04-21-pbi-sequence-implementation.md`.
- `[[2026-04-24-mu2e-aitools-skills]]` — referenced 4× from
  `metacat-reference`. No page or raw file with that slug.
  **Fix:** create the page or remove the references.
- `[[run1bai-campaign]]` — referenced 1× (likely from `lint-2026-04-21`'s
  initial example). No page exists.
  **Fix:** remove the reference (it was a placeholder).

## 🟡 Orphan Pages

None. All 13 pages in `wiki/pages/` have ≥ 1 inbound reference.
`json2jobdef-staging-workflow` (added today) has 1 inbound from
`wiki/index.md` under Sources.

## 🟡 Contradictions

None detected this pass. (Sweep was shallow — no semantic diff against
prior pages; flag if you want a deeper pass next lint.)

## 🟡 Stale Claims

None flagged this pass. Prior lint reports (`lint-2026-04-22`) noted
"stale overview claim" — `wiki/overview.md` not re-audited today.
Re-audit on next lint.

## 🔵 Missing Concept Pages

- `[[2026-04-21-pbi-sequence-implementation]]` — 8 references over
  3 weeks; the raw research is rich enough to deserve a published
  synthesis. Suggested action: `/wiki-ingest raw/2026-04-21-pbi-sequence-implementation.md`.
- `[[2026-04-24-mu2e-aitools-skills]]` — 4 references; if the
  underlying topic still matters, write it; if not, scrub the refs.

## 🔵 Coverage Gaps

- The new `json2jobdef-staging-workflow` page links to four memory
  files via prose (e.g. "memory `reference_reco_dbservice_overrides.md`")
  rather than `[[slug]]` syntax — the `[[…]]` convention only resolves
  under `wiki/pages/`. Consider a SCHEMA addendum or convention note
  for "linking to memory" so future pages don't repeat the broken-link
  mistake I made on first draft.

## What changed

- Added `wiki/pages/json2jobdef-staging-workflow.md` (cross-stage
  staging-config reference).
- Added `.claude/commands/stage-entry.md` (slash skill wrapping
  `json2jobdef --json data/<campaign>/<stage>.json …`).
- Added memory pointer `reference_json2jobdef_staging_workflow.md` +
  `MEMORY.md` index line.
- Updated `wiki/index.md` Sources with the new page.

## Concrete fixes available

I will offer fixes for the 3 broken links only on request — they are
not regressions from today's ingest.
